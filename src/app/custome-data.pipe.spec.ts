import { CustomeDataPipe } from './custome-data.pipe';

describe('CustomeDataPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomeDataPipe();
    expect(pipe).toBeTruthy();
  });
});
