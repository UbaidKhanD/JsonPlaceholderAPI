import { MathCalculationPipe } from './math-calculation.pipe';

describe('MathCalculationPipe', () => {
  it('create an instance', () => {
    const pipe = new MathCalculationPipe();
    expect(pipe).toBeTruthy();
  });
});
